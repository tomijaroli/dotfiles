return {
  "ray-x/lsp_signature.nvim",
  event = "InsertEnter",
  opts = {
    bind = true,
    floating_window = true,
    hint_enable = false,
    -- hint_prefix = " ",
    handler_opts = { border = "rounded" },
  },
}
