# LLDB Configuration

macOS-only debugger configuration for Xcode/Swift development. Automatically loaded when debugging.

**Custom command:** `json` - Pretty-prints JSON data in the debugger  
**Settings:** Disables Swift/C++ interoperability for stability

Stowed to `~/.lldbinit`. Add custom commands or settings as needed.
